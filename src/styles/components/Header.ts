import { styled } from '..'

export const HeaderContent = styled('div', {
    display: 'flex',
    padding: '2rem 0',
    width: '100%',
    maxWidth: 1180,
    margin: '0 auto',
    justifyContent: 'space-between',
    alignItems: 'center',
})