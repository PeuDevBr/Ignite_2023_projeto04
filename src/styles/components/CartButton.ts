import { styled } from '..'

export const Button = styled('button', {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '$gray800',
    border: 'none',
    height: '3rem',
    width: '3rem',
    borderRadius: 8,
    cursor: 'pointer',


})